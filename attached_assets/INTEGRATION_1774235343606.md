# Services Section — Integration Instructions
# =============================================
# Two changes to make in client/src/pages/home.tsx

## CHANGE 1 — Add import at the top of home.tsx
# Add this line with your other imports (around line 1-15):

import { ServicesSection } from "@/components/home/ServicesSection";


## CHANGE 2 — Replace the existing services section
# Find this block in home.tsx (around line 208-265):

      <section className="bg-[#f5f7fb] py-20 text-slate-900">
        <div className="mx-auto max-w-7xl px-6 lg:px-8">
          <div className="max-w-3xl">
            <p className="text-sm font-semibold uppercase tracking-[0.3em] text-cyan-700">Services</p>
            ...
            {serviceCards.map(...)}
            ...
          </div>
        </div>
      </section>

# Replace the ENTIRE block (from <section> to </section>) with:

      <ServicesSection />


## CHANGE 3 — Remove the serviceCards constant (optional cleanup)
# The old serviceCards array at the top of home.tsx is no longer needed.
# You can safely delete it — it's been moved into ServicesSection.tsx.
# It looks like:
#
#   const serviceCards = [
#     { title: "License & Permit Surety Bonds", description: "..." },
#     { title: "Commercial Surety Bonds", description: "..." },
#     ...
#   ];


## WHAT CHANGED

### Before (3 generic cards, no links, no BMC-84):
- License & Permit Surety Bonds — text only
- Commercial Surety Bonds — text only
- Contract Surety Bonds — text only

### After (6 cards, all linked, BMC-84 featured):
1. Contract Surety Bonds      → /construction         (bid/performance/payment)
2. License & Permit Bonds     → /bonds/license-bond-texas
3. Commercial Surety Bonds    → /quote
4. BMC-84 Freight Broker Bond → /bonds/bmc-84-freight-broker  ← NEW (highlighted with "New service" badge)
5. Court Bonds                → /quote
6. Miscellaneous Surety       → /quote

### New design features:
- Every card links to its dedicated page or quote form
- BMC-84 card has indigo highlight + "New service" badge to draw attention
- Each card has 3 bullet points summarizing what's included
- Bottom gradient callout: "Don't see the bond you need?" → AI Bond Finder + Get Quote
- Compliance disclaimer at the bottom (already present in footer, reinforced here)
- Hover state: cards lift slightly on hover, arrow animates right
- Responsive: 1 col mobile → 2 col tablet → 3 col desktop
